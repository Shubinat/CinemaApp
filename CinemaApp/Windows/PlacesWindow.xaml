<Window x:Class="CinemaApp.Windows.PlacesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaApp.Windows"
        mc:Ignorable="d"
        Title="Выбор места" MinHeight="450" MinWidth="800">
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <ScrollViewer Margin="5">
            <StackPanel x:Name="Hall" HorizontalAlignment="Center" VerticalAlignment="Center">

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1" Background="#19FFFFFF" x:Name="GridEmpty" BorderThickness="1" BorderBrush="Gray" Margin="5">
            <StackPanel >
                <TextBlock Foreground="White" Margin="5" VerticalAlignment="Center" FontSize="16" x:Name="TBlockInfo"/>
                <TextBlock Foreground="White" Margin="5" VerticalAlignment="Center" FontSize="16" x:Name="TBlockPrice"/>
                <TextBlock Foreground="White" Margin="5" VerticalAlignment="Center" FontSize="16" x:Name="TBlockCategory"/>
            </StackPanel> 
        </Border>
    </Grid>
</Window>
